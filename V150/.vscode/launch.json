{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Editor UI (Tauri Dev)",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run tauri:dev",
            "args": [],
            "cwd": "${workspaceFolder}\\editor-ui",
            "console": "integratedTerminal"
        },
        {
            "name": "Build Editor UI (Tauri)",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run tauri:build",
            "cwd": "${workspaceFolder}\\editor-ui",
            "console": "integratedTerminal"
        }
    ]
}
