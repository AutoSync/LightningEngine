# Projetos e Templates

Baseado no roadmap v0.15.0, com status unificado por contexto.

## Concluído

- Modelos de exemplo
  - Empty
  - 2D
  - 3D
- Estrutura de pastas criada automaticamente
- `project.ini`, `editor.ini`, `game.ini`
- `.gitignore` padrão
- Cena vazia padrão (`scenes/main.lescene`)
- Histórico de projetos recentes (`%APPDATA%/LightningEngine/recent.ini`)

## Pendente

- Normalização de assets para formato `.leasset`
