# Renderização

Contexto de Render

- [x] Render 2D (shapes + textures + alpha blend)
- [x] Render 3D (pipe3D, DrawMesh, View/Projection/Light, depth buffer)
- [ ] PostFX
- [ ] PostFX expandido com Equinox (bloom, motion blur, depth of field, etc)
